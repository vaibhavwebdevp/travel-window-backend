PORT=3000
# MongoDB – Local / custom:
# MONGODB_URI=mongodb+srv://user:pass@cluster.mongodb.net/dbname?retryWrites=true&w=majority
# Or: MONGODB_URI=mongodb://localhost:27017/travel_agency
# Vercel (MongoDB integration): travel_window_MONGODB_URI is set in Vercel → Env Vars; code uses it automatically.
JWT_SECRET=your_jwt_secret_key_here_change_this_in_production
# Production (Vercel): run DB seed once via GET /api/seed?secret=YOUR_SECRET. Set same value in Vercel env.
SEED_SECRET=your_seed_secret_change_in_production
NODE_ENV=development
# Optional: reCAPTCHA v2 secret key (from Google reCAPTCHA admin). If set, login requires valid captcha.
# RECAPTCHA_SECRET=your_recaptcha_secret_key
